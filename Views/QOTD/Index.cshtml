@model IEnumerable<HomeBase.Models.QOTD>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Question)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.QuestionAuthor)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.QuestionDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Theme)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Question)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionAuthor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Theme)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>|
                <a asp-controller="QOTDAnswer" asp-action="Create" asp-route-id="@item.ID">ADD Answer</a>-->
            </td>
        </tr>
}
    </tbody>
</table>
